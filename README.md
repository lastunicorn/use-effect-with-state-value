# React `useEffect` Demo (Document Title)

Demo application that shows how to use the `useEffect` hook in React.

There are multiple demo repositories for `useEffect`. This one is updating the browser's title text which contains a number incremented/decremented by the user. 

Uses Vite as build tool.

See the [Tutorial](/doc/tutorial/README.md) for more details.

